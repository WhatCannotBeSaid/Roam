---
name: roam
description: Roam Research 项目的完整视觉系统与 CSS 规则。整合了排版（Typography）、视觉系统（Visual System）及 CSS 属性保留规则。使用场景：编辑 roam.css、Roam JS 块、主题变量、暗色模式、Roam UI/UX 样式、以及任何与 roam.css / blueprint.css / site.css 对标的 CSS 文件。
---

# Roam — 排版 / 视觉系统 / CSS 规则

> **本技能整合以下子技能**：
> - `typography`（通用排版与视觉系统标准）
> - `roam-research-visual-system`（Roam 项目专有视觉系统）
> - `roam-css-properties`（CSS 属性保留规则）
>
> 执行时须同时遵循三者的所有约束，优先级：CSS 属性保留规则 > Roam 专有视觉系统 > 通用排版标准。

---

## 一、设计哲学（上层原则）

- **少即是多**：不随意堆砌字体、颜色或特效；过多强调等于没有重点。
- **协调与风格统一**：视觉好看的重点是「协调」；风格混搭无章会变成大杂烩，宜控制在 1～2 套视觉风格内。
- **层级清晰**：通过**大小、粗细、颜色**区分主次（标题 / 副标 / 正文），而非处处都像重点。
- **慎用特效**：不熟悉的特效（阴影、渐层等）能少用就少用；优先保证清晰与可读。

**「无边界、沉浸式」设计**：以内容为中心，少边框、分割线、卡片描边；内容与背景自然过渡；减少视觉割裂与界面「框感」。

---

## 二、字体 (Font)

### 2.0 字体值锁定（铁律）

- **已定义的字体栈不得修改、替换或重定义**：包括 `font-family` 字体变量（如 `--body-font`、`--heading-font`、`--code-font`、`--ui-font` 等）及其值，以及具体选择器上已写死的 `font-family`。
- 新增样式**只能引用**既有字体变量 / 字体栈，不得自创字体栈或改写已有字体栈的成员 / 顺序。
- **字号**同理：已有 `font-size` 变量（`--fs-*`）与值不得随意调整。
- 仅当用户**明确要求**修改字体 / 字号时，方可动。否则任何涉及重构、清理、优化的改动必须保持字体与字号的最终计算值不变。

### 2.1 font-family 书写顺序（铁律）

1. **西文字体在前，中文字体在后**  
   浏览器按字符逐项匹配；西文优先西文字体，中文/CJK 用后续字体。
2. **效果佳的在前，效果一般的在后**
3. **末尾必须指定通用字体族**：`serif` / `sans-serif` / `monospace`

```css
/* ✓ */
font-family: 'Palatino', '方正屏显雅宋', serif;

/* ✗ */
font-family: '方正屏显雅宋', 'Palatino';   /* 顺序反；无通用族 */
```

### 2.2 字体名称规范

- 含空格或中文的字体名必须用引号
- 使用系统/字体文件中注册的准确名称；商业字体可写 2～3 个常见变体（如「简 R」「简」）及英文内部名以提高匹配率

### 2.3 缺字、粗体与易读性

| 问题 | 要求 |
|------|------|
| **缺字** | 字体栈须包含覆盖目标语言的 fallback（如 Noto Sans CJK）；上线前检查缺字 |
| **粗体** | 优先选用字体家族自带的粗体字型，避免 `font-weight: bold` 强行加粗 |
| **字号** | 正文最小不低于 `0.875rem`，须考虑移动端可读性 |
| **对比度** | 正文与背景须有足够对比度，避免「颜色太相近」 |

### 2.4 字体角色与变量

建议按**角色**划分字体变量（如 title / heading / body / quote / tag / code / ui），避免 A 角色字体误用于 B 角色；正文区与界面控件区应区分。

---

## 三、配色 (Color)

### 3.1 变量化（色值锁定）

- **色值来源**：色值仅来自 https://colors.masantu.com/#/
- **已定义的色值不得修改、替换或重定义**：包括既有配色变量（如 `--m-*`、`--text-color`、`--bg`、`--primary` 等）及其值，以及具体选择器上已写死的颜色。
- 新增样式**只能引用**既有色值变量，不得自创新色值或改写既有变量的 L/C/H。
- 亮/暗模式共用变量名，仅在主题类下修改变量值；变量引用须带 fallback。
- 仅当用户**明确要求**调整配色时，方可动。否则任何涉及重构、清理、优化的改动必须保持配色最终计算值不变。

### 3.2 对比度（APCA 标准）

| 模式 | 正文 Lc 建议 |
|------|--------------|
| 日间（正极性） | Lc ≥ 75（强光环境建议 80） |
| 夜间（负极性） | Lc ∈ [60, 80]；**禁止纯白文字**（`#FFFFFF`） |

**修正策略**（若 Lc 不达标）：
1. 保持色相（Hue）不变。
2. 沿同一色相轴线调整明度（Lightness）：日间可加深文字或减浅背景；夜间可减浅文字或加深背景。
3. 重新计算 Lc 直至满足阈值。

### 3.3 色相协调

- **主色/辅助色**：类比色（ΔH ≈ 30°）
- **功能/强调色**：分裂补色，保证辨识度
- **中性色**：可注入少量主色饱和度，避免纯灰

### 3.4 交互态

- hover / active / focus 颜色优先用 `oklch()` 从主变量派生（调 L 或 C），避免硬编码新色

---

## 四、CSS 属性保留规则（铁律 — roam.css 专用）

编辑 `roam.css`（及本仓库内与 `blueprint.css` / `site.css` 对标的 CSS）时，对选择器的属性**必须严格执行**以下保留规则。

### 4.1 只保留两类属性

| 类别 | 允许的属性 |
|------|-----------|
| **Color 类** | `color`, `background` / `background-color`（仅颜色）, `border-color`, `fill`, `stroke` |
| **Font 类** | `font-family`, `font-size` |

### 4.2 一律删除

所有**非 color、非 font-family/font-size** 的属性都必须删除，例如：

- **布局**：`display`, `position`, `z-index`, `flex`, `grid`, `width`, `height`, `margin`, `padding`, `left`, `right`, `top`, `bottom`, `inset`
- **边框/圆角/阴影**：`border`（width/style）、`border-width`, `border-style`, `border-radius`, `border-left` 等简写、`box-shadow`
- **字体其它**：`font-weight`, `font-style`, `letter-spacing`, `line-height`, `text-decoration`
- **效果/动画**：`opacity`, `filter`, `transform`, `transition`, `animation`
- **其它**：`overflow`, `cursor`, `visibility`, `clip-path` 等

若某条规则经修剪后**没有任何可保留属性**，则整条规则删除，可留注释说明「仅版式，已移除」。

### 4.3 原则

- **版式、结构、动效**交给 `blueprint.css` / `site.css` 或 Roam 默认样式；`roam.css` 只做**配色与字体（family/size）**覆盖。
- 先改展示模式，再同步编辑模式，保持所见即所得。
- 字体与配色修改须同时遵循**排版（Typography）**与**视觉系统（Visual System）**的约束。

---

## 五、Roam Research 视觉系统 — 专有约束与资源

### 5.1 核心协议

#### 锁定项（铁律）

- **配色值锁定**：已定义的配色变量（`--m-*`、`--text-color`、`--bg` 等）与选择器上的色值**不得修改、替换或重定义**；新增样式**只能引用**既有变量。色值来源仅限 https://colors.masantu.com/#/ 。
- **字体值锁定**：已定义的字体栈变量（`--body-font`、`--heading-font`、`--code-font`、`--ui-font` 等）与选择器上的 `font-family` **不得修改、替换或重定义**；新增样式**只能引用**既有变量；字号（`--fs-*`）同理。
- **属性范围锁定**：`roam.css` 仅允许 color 类 + `font-family` + `font-size`（详见第四章 CSS 属性保留规则）。
- **唯一解锁方式**：用户**明确要求**修改字体 / 配色时才可动；任何重构、清理、优化、新增 DOM 适配都必须保持字体与配色的最终计算值不变。

#### 可修改

- 在**不改动既有字体/配色值**的前提下：新增选择器引用既有变量、补齐未主题化的 DOM、合并重复规则、清理注释与空白、调整变量逻辑（非色值/非字体值）、性能优化等。
- 版式、结构、间距、行高、动效**不在 `roam.css` 作用域内**，交由 `blueprint.css` / `site.css` / Roam 默认控制。

### 5.2 样式约束

- **DOM 匹配**：选择器须严格匹配 `index.html` 中实际存在的类/ID/结构，严禁猜测。

### 5.3 配色细节（Roam 实现）

- **变量**：`--m-` 前缀；`:root` / `.rm-dark-theme` 下定义；引用须带 fallback。
- **APCA**：日间 Lc ≥ 75；夜间 Lc ∈ [60, 80]，严禁纯白文字。
- **交互态**：优先用 `oklch()` 从 `--m-` 变量派生。

### 5.4 设计风格

- **无边界**：少边框、分割线、卡片描边；内容与背景自然过渡。
- **沉浸式**：少 chrome、装饰元素；留白与内容一体，满幅/全屏体验。

### 5.5 Roam 专有资源

- **色值来源**：仅 https://colors.masantu.com/#/ ；变量 `--m-` 在 `:root` / `.rm-dark-theme` 下定义；作用域 `.roam-app`。
- **滚动条与选中**：须使用 `--m-` 变量，不得硬编码。
- **APCA 细则**：见参考章节。

---

## 六、排版 (Layout & Type)

### 6.1 层级

- 标题、副标、正文应区隔
- 通过 **大小、粗细、颜色** 营造主次差异

### 6.2 对齐

- 对齐使视觉更整齐、好阅读
- 先排整齐再考虑变化

### 6.3 留白

- 信息与信息之间宜留空间
- 区块边界与文字之间需留白，过挤会降低舒适度

### 6.4 行高

- 正文建议 ≥ 1.5，长文可 1.6～1.75

---

## 七、工程与性能

- 避免 `!important`；优先通过选择器特异性提升权重
- 避免大量 `filter: blur()` 或复杂多层阴影
- 若有 MutationObserver 等 JS，须避免造成打字/输入延迟，优化 Observer 回调

---

## 八、评估检查清单

### 锁定原则（铁律，优先级最高）

- [ ] 用户**明确要求**修改字体 / 配色了吗？若否，下方 diff 里 **没有** 既有 `font-family` / 色值 / 字体变量 / 配色变量的替换或重定义？
- [ ] 新增样式**只引用**既有字体 / 色值变量，没有自创字体栈或新色值？
- [ ] diff 中没有任何非 color / non-font-family/font-size 的属性（roam.css 场景）？

### 字体（仅当用户要求新增 / 修改字体时适用）

- [ ] font-family 顺序：西文 → 中文/CJK → 通用族？
- [ ] 含空格/中文的字体名加了引号？
- [ ] 字体栈含目标语言 fallback？无明显缺字？
- [ ] 需粗体时用字体家族自带字型而非 CSS 加粗？

### 配色（仅当用户要求新增 / 修改配色时适用）

- [ ] 色值来自 colors.masantu.com？
- [ ] 颜色已变量化且引用带 fallback？
- [ ] 正文/背景对比度达标（日间 Lc ≥75，夜间 60–80）？
- [ ] 交互态用 oklch() 从主变量派生？

### 排版

- [ ] 层级清晰（大小/粗细/颜色有主次）？
- [ ] 字号在移动端可读（最小 0.875rem）？
- [ ] 留白与对齐合理？

### 一致性

- [ ] 展示模式与编辑模式已同步（若适用）？
- [ ] 符合项目约定的属性范围（roam.css 仅 color + font-family + font-size）？
- [ ] 日夜切换无闪烁 / 位移？
- [ ] 无 MutationObserver 导致的打字延迟？
- [ ] 保持无边界、沉浸式风格？

---

## 参考

### APCA 极性感知对比度

APCA（Accessible Perceptual Contrast Algorithm）按「正极性」（深字浅底）与「负极性」（浅字深底）分别计算 Lc（perceived contrast）。

- **日间（正极性）**：正文为深色、背景为浅色。目标 Lc ≥ 75，强光环境建议 Lc 80。
- **夜间（负极性）**：正文为浅色、背景为深色。目标 Lc 在 60–80；禁止使用纯白 `#FFFFFF` 作为正文。

实际计算可参考 [APCA 官方](https://www.myndex.com/APCA/) 或使用 npm 包 `apca-w3`。校验时输入前景与背景的 sRGB 值，得到 Lc 数值后与阈值对比。

### 色相环协调

- **主色与辅助色**：类比色，色相差约 ΔH ≈ 30°（例如主色竹青，辅助色秋香）。
- **功能/强调色**（TODO、重要标签等）：分裂补色，与主色形成晰区分。
- **中性色**：背景、边框等在灰阶基础上注入 3%–8% 主色饱和度（例如用带微量主色相的灰色，而非纯 #808080）。

### 色彩来源

所有色值仅从 **https://colors.masantu.com/#/** 选取；变量名与语义按项目约定在 `:root` 与主题类下统一管理。

---

**参考来源**：kevin_learn（Threads）— 少即是多、慎用特效、有重点；缺字/乱加粗/易读性；风格统一；对齐/层级/留白。APCA 对比度标准。